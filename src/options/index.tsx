import React from "react";
import ReactDOM from "react-dom";
import "./options.scss";

const OptionsPage = () => {
  return <h1>No Options Defined Yet</h1>;
};

ReactDOM.render(
  <React.StrictMode>
    <OptionsPage />
  </React.StrictMode>,
  document.getElementById("root")
);
